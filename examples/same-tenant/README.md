# VPC Peering on in same tenant

Create a VPC peering between 2 VPC located in the same tenant

## Usage

Fil in the required parameters into `main.tf`

Then run the Terraform commands:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```
